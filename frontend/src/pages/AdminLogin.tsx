import { ArrowLeft, LockKeyhole, ShieldCheck, UserRound } from "lucide-react";
import { useState, type FormEvent } from "react";
import { Link, Navigate, useNavigate } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";
export default function AdminLogin(){const{name,session,login}=(()=>{const a=useAuth();return{name:a.session?.name,session:a.session,login:a.login}})();const navigate=useNavigate();const[adminName,setAdminName]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[pending,setPending]=useState(false);if(session)return <Navigate to="/admin/dashboard" replace/>;async function submit(e:FormEvent){e.preventDefault();setPending(true);setError("");try{await login(adminName,password);navigate("/admin/dashboard");}catch(c){setError(c instanceof Error?c.message:"Login failed");}finally{setPending(false)}}return <main className="admin-login-page"><div className="login-grid" aria-hidden="true"/><Link to="/" className="login-back"><ArrowLeft/>Back to portal choice</Link><section className="admin-login-card"><span className="login-shield"><ShieldCheck/></span><p>AUTHORIZED PORTAL</p><h1>AWS Management Access</h1><span>Authorized access to SkyGuard sensor intelligence and station controls.</span><form onSubmit={(e)=>void submit(e)}><label><span>Administrator Name</span><div><UserRound/><input autoComplete="username" value={adminName} onChange={e=>setAdminName(e.target.value)} required/></div></label><label><span>Password</span><div><LockKeyhole/><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div></label>{error&&<p className="form-error">{error}</p>}<button disabled={pending}>{pending?"Signing in…":"Sign In"}</button></form><aside><strong>Prototype Demo Access</strong><span>Administrator: Gaurav Sethia</span><small>Demo credentials only. Replace before production deployment.</small></aside></section></main>}
